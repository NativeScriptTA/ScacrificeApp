<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded" cssFile="~/name-list.css">
    <ListView items="{{ names }}" tap="{{ nameTapCommand }}">
        <ListView.itemTemplate>
                <Label class="name-list-item" tap="{{ nameTapCommand }}" text="{{ $value }}"></Label>
        </ListView.itemTemplate>
    </ListView>
</Page>
